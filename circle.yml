machine:
  pre:
    - sed -i 's/github/git-non-exist-hub/g' ~/.gitconfig

dependencies:
  pre:
    - curl -L https://sh.rustup.rs | sh -s -- -y

test:
  override:
    - cargo build --release
    - cargo test
